# Create env with:
# `conda env create -f patate-env.yml`
# Then activate conda env:
# `source activate patate`

name: patate
channels:
  - conda-forge
dependencies:
  - python=3.6
  - pip
  - django=2.2
  - django-extensions
  - djangorestframework
  - djangorestframework-jwt
  - pillow
  - service_identity
  - numpy
  - pandas
  - tensorflow=1.9.0
  - keras=2.1.5
  - h5py
  - elasticsearch
  - boto3
  - matplotlib
  - pytest
  - pip:
    - channels
    - adafruit-pca9685

# XboxControler:
# sudo apt-get install xboxdrv
